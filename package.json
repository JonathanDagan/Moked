{
    "name": "moked",
    "version": "0.2.0",
    "private": true,
    "scripts": {
      "dev": "yarn --cwd frontend run dev",
      "lint": "yarn --cwd frontend run lint",
      "open-ct": "yarn --cwd frontend run cypress open-ct",
      "postinstall": "(cd frontend && yarn install);"
    },
    "dependencies": {
    },
    "devDependencies": {
      "husky": "^7.0.0"
    }
  }
  